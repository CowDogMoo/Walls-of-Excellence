---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sliver
  namespace: c2
spec:
  ingressClassName: ingress-traefik
  rules:
    - host: &host sliver.techvomit.xyz
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: sliver
                port:
                  number: 80
  tls:
    - hosts:
        - *host
      secretName: techvomit-xyz-production-tls
