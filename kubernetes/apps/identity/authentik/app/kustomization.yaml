---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: identity
resources:
  - externalsecret.yaml
  - externalsecret-admin.yaml
  - authentik-oauth-blueprint.yaml
  - authentik-passkeys-blueprint.yaml
  - helmrelease.yaml
  - helmrepository.yaml
  - prometheusrule.yaml
  - group-sync-job.yaml
labels:
  - pairs:
      app.kubernetes.io/name: authentik
      app.kubernetes.io/instance: authentik
