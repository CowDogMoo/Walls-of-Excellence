---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Flux-Kustomizations
  - ./nfs-subdir-external-provisioner/ks.yaml
  - ./config-syncer/ks.yaml
