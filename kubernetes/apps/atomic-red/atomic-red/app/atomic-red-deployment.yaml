---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atomicred
  namespace: atomic-red
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atomicred
  template:
    metadata:
      labels:
        app: atomicred
    spec:
      containers:
        - name: atomicred
          image: ghcr.io/cowdogmoo/invoke-atomicredteam:latest
          command: ["sleep", "3560d"]
          securityContext:
            privileged: true
          resources:
            limits:
              cpu: "1"
              memory: "1Gi"
      nodeSelector:
        kubernetes.io/os: linux
