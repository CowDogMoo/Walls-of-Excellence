{{- $values := (fromYaml (readFile (printf "../../../apps/%s/%s/app/helmrelease.yaml" .Release.Namespace .Release.Name))).spec.values -}}
{{- if and (eq .Release.Name "flux-instance") (env "FLUX_BRANCH") -}}
{{- $_ := set $values.instance.sync "ref" (printf "refs/heads/%s" (env "FLUX_BRANCH")) -}}
{{- end -}}
{{ $values | toYaml }}
